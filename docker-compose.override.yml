version: "3.8"

services:
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: trailmandu-redis-commander
    restart: always
    environment:
      - REDIS_HOSTS=local:redis:6379
    ports:
      - "8081:8081"
    depends_on:
      - redis
